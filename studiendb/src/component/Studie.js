// import React from 'react'
const Studie = ({ titel, studienleiter, details, zeitraum, stunden, kriterien }) => {
    return (
      <div>
          {titel} {studienleiter} {details} {zeitraum} {stunden} {kriterien}
      </div>
    )
  }
  export default Studie